# Optional — only needed to upload encrypted blobs to S3. The encrypt/decrypt
# tool works fully client-side with none of these set.
AWS_ACCESS_KEY_ID=""
AWS_SECRET_ACCESS_KEY=""
AWS_REGION="us-east-1"
S3_BUCKET=""
